# 日ノ本異聞録 外部運用PWA v0.50

これは、ChatGPT外で「日ノ本異聞録：歴史の継承者」の状態・正史ログ・AI返答検査を管理するための最小プロトタイプです。

## 方針

- AIは文章案だけ作る
- PWAが状態を保持する
- PWAがAI返答を検査する
- ユーザーが採用した返答だけ正史ログへ入る
- 保存済み・Drive反映済み・正史確定はAIに言わせない

## 入っているファイル

- `index.html`：画面本体
- `style.css`：見た目
- `app.js`：状態管理・プロンプト生成・返答検査・採用/却下
- `manifest.json`：PWA設定
- `service-worker.js`：オフラインキャッシュ
- `initial-state.json`：初期状態
- `icon.svg`：PWAアイコン

## 使い方

1. GitHubで新しいリポジトリを作る
2. このZIPの中身をアップロードする
3. GitHub Pagesを有効化する
4. Android ChromeでPagesのURLを開く
5. 「ホーム画面に追加」またはインストールする

## 基本操作

1. PWAで「プロンプト生成」
2. 生成された内容をChatGPTへ貼る
3. ChatGPT返答をPWAへ貼り戻す
4. 「返答検査」
5. 重大警告がなければ「採用して正史ログへ」
6. 重大警告があれば却下または修正して再検査

## 初期状態

- 2026年4月11日（土）朝
- 現在地：仙台城方面へ徒歩移動中
- 富士割：仙台住居の富士割祭壇
- 富士抜き：本道神社本社奉納
- 政宗：返信待ち
- 足利輝統：返信待ち
- 作戦室：新規確認なし、通常任務受諾なし、出動なし
- 敵状態：NO-CONTACT

## 注意

このMVPは最小版です。今後追加するなら、以下が候補です。

- 通知スレッド個別管理
- 任務ステージ管理
- 富士割持ち出し確認UI
- 不可逆ゲート確認カード
- chat_message形式の構文検査強化
- 正史ログの章管理
- Google Drive / GitHub連携


## v0.1.1

- 事故防止チェック文の『戦闘UI不可』を重大警告にしないよう修正
- 『返信待ち』という安全確認だけでは自動返信警告にしないよう修正
- スマホを持っている描写だけでは通知形式警告を出さないよう修正


## v0.2 追加内容

- 行動入力欄を追加
- 「送信してプロンプト生成」ボタンを追加
- 送信履歴を追加
- 今回のプレイヤー行動をAIプロンプトへ自動挿入
- ChatGPT返答を貼り戻すと自動検査
- 採用ログにプレイヤー行動も保存
- v0.1のローカル保存を見つけた場合は自動引き継ぎ

## v0.2の使い方

1. 行動入力欄に行動を書く
2. 「送信してプロンプト生成」を押す
3. 自動コピーされたプロンプトをChatGPTへ貼る
4. ChatGPTの返答をPWAの返答欄へ貼る
5. 自動検査の結果を見る
6. 問題なければ「採用して正史ログへ」


## v0.25 追加内容

- Firefox拡張連携の準備欄を追加
- 送信時に `hinomoto_bridge_outbox` へプロンプトを保存
- `window.postMessage` で拡張へ PROMPT_READY を通知
- 拡張から `HINOMOTO_EXTENSION / AI_RESPONSE` を受け取る入口を追加
- `hinomoto_bridge_inbox` のポーリング受信を追加
- 拡張返答を受け取ったら返答欄へ自動入力し、自動検査
- 採用は引き続き手動
- 「返答受信テスト」ボタンで拡張なしでも受信処理を確認可能

## v0.25の意味

これはFirefox拡張そのものではありません。
次に作るFirefox拡張 v0.3が、PWAとChatGPT Webをつなぐための受け口です。


## v0.26 追加内容

- 「採用して正史ログへ」の連打対策
- 同じ requestId / 同じ本文の二重採用を自動拒否
- 正史ログの「重複ログ整理」ボタンを追加
- v0.25の保存データを自動引き継ぎ

## 連打事故が起きた場合

1. v0.26へ更新
2. PWAを開く
3. 正史ログの「重複ログ整理」を押す
4. 重複分だけ削除されます

採用は引き続き手動です。


## v0.27 追加内容

スマホで見やすいUIへ整理しました。

- 下部タブナビを追加
- 進行 / 状態 / AI / ログ / 設定 に画面を分割
- 現在地・富士割・敵状態・拡張状態を上部カードに集約
- 送信→返答→検査→採用の進行バーを追加
- 本文と行動候補を見やすく整理
- 正史ログをログタブに分離
- v0.26の重複採用防止は維持
- Firefox拡張連携は維持


## v0.28 追加内容

v0.27でHTMLだけ更新され、古いapp.js/CSSと混ざって「読み込み中」のまま止まる事故への対策版です。

- `app.js` を `app-v028.js` に変更
- `style.css` を `style-v028.css` に変更
- index.htmlから新ファイル名で読み込み
- Service Workerのキャッシュ名をv028へ変更
- 古いキャッシュを削除しやすくする


## v0.29 追加内容

ChatGPTで生成された本文を読みやすくするため、進行タブの「行動を書く」の上に「最新AI本文」カードを追加しました。

- ChatGPT返答がPWAへ戻ったら、最新AI本文カードへ全文表示
- 未採用 / 採用済みバッジを表示
- 最新AI本文カードから「この本文を採用」可能
- 返答受信時に最新AI本文カードへ自動スクロール
- v0.28のキャッシュ混在対策は維持


## v0.30 追加内容

最新AI本文が小さくて読みにくい問題への改善版です。

- 最新AI本文カードの表示領域を拡大
- 本文の行間と文字サイズを読みやすく調整
- 「大きく読む」ボタンを追加
- 全画面の読書モードを追加
- 読書モードからそのまま採用可能
- v0.29の最新本文表示、v0.28のキャッシュ対策、v0.26の重複採用防止は維持


## v0.31 追加内容

AI返答を採用するときに、返答全体をそのまま現在本文へ入れないようにしました。

- AI返答を「事故防止チェック」「本文案」「行動候補」に自動分解
- 最新AI本文カードには本文案を中心に表示
- 採用時、現在本文には本文案だけ保存
- 行動候補は自動で新しい候補へ更新
- 正史ログにはAI返答全文、本文案、事故防止チェック、行動候補を保存
- 次回プロンプトの【現在本文】が汚れにくくなります


## v0.32 追加内容

v0.31で「最新AI本文」が大きく、行動入力が見失われる問題への改善版です。

- 最新AI本文カードを少しコンパクトに調整
- 「次の行動へ」ボタンを追加
- 採用後、自動で行動入力カードへスクロール
- 採用済み本文カードを折りたたみ気味に表示
- v0.31の本文案だけ保存・行動候補自動更新は維持


## v0.33 追加内容

AI返答を受信した瞬間、最初から大きな読書モードで表示するようにしました。

- ChatGPT返答がPWAへ戻ったら自動で読書モードを開く
- 読書モードの文字サイズと行間をさらに拡大
- 通常カード側は少しコンパクト化
- 読んだあと、そのまま採用可能
- 採用後は次の行動入力へ戻る流れを維持


## v0.34 追加内容

v0.33で「自動読書モード」にはしたものの、通常画面の最新AI本文カードが小さいままだった問題への修正版です。

- 通常画面の最新AI本文カードを大幅に拡大
- 文字サイズと行間を読書モード寄りに調整
- ボタンは本文カード下部に残して採用しやすくした
- 返答受信時は全画面読書モードではなく、拡大された本文カードへ誘導
- 「大きく読む」ボタンは引き続き使用可能


## v0.35 追加内容

「大きく読む」を押さなくても、最新AI本文を通常画面で大きく表示する版です。

- 「大きく読む」ボタンを削除
- 読書モードを標準では使わない
- 最新AI本文カードを最初から大きく表示
- ボタンが本文の上に重なる問題を修正
- ボタンは本文の下に通常配置
- v0.31の本文案だけ保存・行動候補自動更新は維持


## v0.36 追加内容

本文スクロール中に下部ナビが被って見える問題への修正版です。

- 「進行 / 状態 / AI / ログ / 設定」の下部固定表示を解除
- タブは画面上側の通常ナビとして表示
- 本文スクロール中に下からコマンドが伸びてくる問題を修正
- 本文の最後まで自然に読めるように調整
- v0.35の本文デフォルト大表示は維持


## v0.37 追加内容

本文を読み終えた時に、そのままページ本体が下へ流れてしまう問題への修正版です。

- 最新AI本文の本文枠だけを独立スクロール領域化
- 本文の最後までスクロールしても、ページ本体へスクロールが伝わりにくいよう調整
- `overscroll-behavior: contain` を追加
- 本文カード下のボタンは本文に被らないまま維持
- v0.36の下部ナビ固定解除、v0.35の本文大表示は維持


## v0.38 追加内容

v0.37で本文枠スクロール固定にした結果、本文表示が読書モードより小さく見える問題への修正版です。

- 最新AI本文を読書モード基準の大きさへ調整
- 本文枠内スクロール固定は維持
- 文字サイズ・行間・余白を大きく調整
- ボタンは本文下に通常表示
- v0.37のページ本体へスクロールが流れにくい挙動は維持


## v0.39 追加内容

本文表示がまだ小さく見える問題への、さらに強い修正版です。

- 未採用のAI本文がある時は「本文優先モード」へ自動切替
- タイトル・ナビ・他カードを一時的に隠して本文領域を最大化
- 文字サイズをさらに拡大
- 本文枠内スクロールは維持
- 採用・返答欄ボタンだけ表示
- 採用後は通常画面へ戻る


## v0.40 追加内容

右スクショの読書モードを正として、未採用AI本文の標準表示にしました。

- 未採用AI本文がある時は読書モード画面を標準表示
- 通常カード型の本文優先モードは使わない
- 上部に「読書モード / 最新AI本文」を表示
- 本文は大きな枠で表示
- 下に「この本文を採用」「返答欄を見る」を表示
- 閉じるボタンで通常画面へ戻れる
- 採用後は通常画面へ戻り、次の行動入力へ進む


## v0.41 追加内容

「閉じる」付きの読書モードを廃止し、通常画面の本文を大きいまま維持する版です。

- 読書モード画面を標準表示にしない
- 閉じる操作を不要化
- 最新AI本文を通常画面で大きく表示
- そのまま下へスクロールして採用ボタン、次の行動入力へ進める
- 未採用本文がある間も画面全体は通常スクロール
- 採用後は次の行動入力へ自然につながる


## v0.42 追加内容

文章をゲーム画面の主役にするためのUI修正版です。

- 未採用本文がある時は、最新AI本文を画面最優先で表示
- 大きなタイトル・タブ・状態カードを一時的に隠す
- 本文の横幅を広く使う
- 文字は大きすぎず、行間広めで読みやすく調整
- 読書モードや閉じる操作は使わない
- 本文の下に採用ボタン
- さらに下に次の行動入力
- 採用後は通常状態へ戻る


## v0.43 追加内容

本文の横幅を広げ、文章をゲーム画面の主役にするための強化版です。

- 未採用本文がある時は、管理UIをほぼ隠して本文を最優先
- 黄色い本文枠を画面幅いっぱい近くまで拡大
- 文字は大きすぎず、横幅を広く使って読みやすく調整
- 本文の下に採用ボタン
- さらに下に次の行動入力
- 読書モードや閉じる操作は使わない


## v0.44 追加内容

本文を読み終えたあとに、採用ボタン以外が見えなくなり、行動入力へ進めない問題への修正版です。

- 未採用本文がある時もバージョン表示は残す
- 管理UIは隠しつつ、本文カードと行動入力カードは必ず表示
- 本文の下に採用ボタン
- その下に次の行動入力を必ず表示
- 「次の行動へ」ボタンで行動入力へスクロール
- 本文の横幅は広く保つ


## v0.45 追加内容

本文の下に採用と次の行動入力が必ず続くようにする固定版です。

- バージョン表示は残す
- 管理UIは隠しつつ、本文カードと行動入力カードは絶対に表示
- 本文の横幅を広く維持
- 本文の直下に「この本文を採用」「次の行動へ」
- その下に行動入力カードを必ず表示
- 読書モードや閉じる操作は使わない


## v0.46 追加内容

本文の下に行動入力が見えているため、「次の行動へ」ボタンを削除しました。

- 本文の下は「この本文を採用」だけに整理
- 「次の行動へ」「返答欄へ」は非表示
- 採用ボタンの下に行動入力が自然につながる
- 文章メインの流れを維持


## v0.47 追加内容

採用しない時の導線を追加しました。

- 本文下に「採用しない」ボタンを追加
- 採用しない場合、AI返答は却下ログへ保存
- 正史本文は更新しない
- 最新AI本文をクリアして行動入力へ戻る
- 同じ行動を書き直して再送、または別行動を書けます


## v0.48 追加内容

AI返答の行動候補をPWA側へ反映する版です。

- 最新AI本文の下に「採用後に反映される行動候補」を表示
- 採用後、行動入力欄の上に候補ボタンを表示
- 候補ボタンを押すと、その行動が入力欄へ入る
- 行動候補がアプリ側に反映されない問題を改善


## v0.49 追加内容

行動候補を別枠ではなく、本文の最後に含めて表示する版です。

- 最新AI本文の最後に `**行動候補**` を表示
- 行動候補の別枠プレビューは非表示
- 採用後の候補ボタン反映は維持
- v0.48の候補ボタン入力機能は維持


## v0.50 追加内容

未採用本文の確認中でもバックアップと設定確認ができるようにしました。

- 未採用本文の下に「JSON出力」ボタンを追加
- 未採用本文の下に「設定へ」ボタンを追加
- 採用/採用しないを押さなくてもバックアップ可能
- 未採用本文は消さずに保持
